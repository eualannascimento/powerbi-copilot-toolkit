{
  "inputs": [
    {
      "id": "fabricToken",
      "type": "promptString",
      "description": "Token do Fabric/Power BI: rode `az account get-access-token --resource https://analysis.windows.net/powerbi/api --query accessToken -o tsv` e cole o resultado (expira em ~60-90 min, peça de novo quando o MCP falhar por 401)",
      "password": true
    }
  ],
  "servers": {
    "FabricIQ": {
      "url": "https://api.fabric.microsoft.com/v1/mcp/fabricaihub/integrations/m365",
      "headers": {
        "X-VARIANTS": "Fabric.Routing.PowerBIDataExploration",
        "Authorization": "Bearer ${input:fabricToken}"
      }
    }
  }
}
